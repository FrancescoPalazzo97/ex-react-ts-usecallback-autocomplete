export type Product = {
    id: number,
    name: string,
    image: string,
    description: string,
    brand: string,
    color: string,
    connectivity: string,
    rating: number,
    wireless: boolean,
    price: number
}